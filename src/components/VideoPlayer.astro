---
const { classes, name } = Astro.props
---

<video
  class:list={[ "rounded-lg absolute z-0 hover:z-10", classes ]}
  autoplay
  loop
  muted
  playsinline
  poster={ `/videos/${name}.optim.avif` }
>

  <source src={ `/videos/${name}.optim.mp4` } type="video/mp4" />
  <source src={ `/videos/${name}.optim.webm` } type="video/webm" />

</video>
