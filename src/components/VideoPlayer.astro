---
const { classes, name, loop = true } = Astro.props
---

<video
  class:list={[ "js-video-player rounded absolute z-0 hover:z-10", classes ]}
  autoplay
  loop={ loop ? loop : null}
  muted
  playsinline
  src={ `/videos/${name}.mp4` }
  >

</video>
